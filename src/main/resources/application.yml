spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bdestate
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
      show-sql: true

  application:
    name: ESTATE-API
