spring:
  datasource:
    url: jdbc:mysql://localhost:3306/estatedb
    username: root
    password: 223925643925
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: update
      show-sql: true

  application:
    name: ESTATE-API
